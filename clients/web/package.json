{
  "name": "pong-web-client",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build-wasm": "wasm-pack build ../../pong_core --target web --out-dir ../web/wasm"
  },
  "dependencies": {
    "peerjs": "^1.5.5",
    "qr-scanner": "^1.4.2"
  },
  "devDependencies": {
    "@types/node": "^20.19.11",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vite-plugin-mkcert": "^1.17.8"
  }
}
